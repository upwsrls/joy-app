// Web stub – returns null (the parent screen renders a list fallback for web)
import React from 'react';

type Props = {
  doni: any[];
  myUserId: string | null | undefined;
  initialRegion: any;
  onMarkerPress: (dono: any) => void;
};

export default function NativeMap(_props: Props) {
  return null;
}
